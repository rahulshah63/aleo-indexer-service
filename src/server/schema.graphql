type Transaction {
  id: ID!
  programId: String!
  functionName: String!
  transactionId: String!
  blockHeight: Int!
  timestamp: String!
  data: String!
}

type Query {
  transactions: [Transaction]
}