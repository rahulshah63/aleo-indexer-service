type Transaction {
  id: ID!
  programName: String!
  functionName: String!
  transactionId: String!
  blockHeight: Int!
  timestamp: String!
  data: String!
}

type Query {
  transactions: [Transaction]
}